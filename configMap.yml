apiVersion: v1
kind: ConfigMap
metadata:
  name: msvc-users
data:
  port: "8001"
  db_host: mysql8:3306
  db_name: msvc_users
  #  db_username: root
  #  db_password: sasa
  courses_url: msvc-courses:8002/api/v1/courses
  application.yml: |-
    config:
      text: Configurando ambiente por defecto
    ---
    spring:
      config:
        activate:
          on-profile: dev
    config:
      text: Configurando ambiente de desarrollo
    ---
    spring:
      config:
        activate:
          on-profile: pro
    config:
      text: Configurando ambiente de produccion

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: msvc-courses
data:
  port: "8002"
  db_host: postgres14:5432
  db_name: msvc_courses
  #  db_username: postgres
  #  db_password: sasa
  users_url: msvc-users:8001/api/v1/users